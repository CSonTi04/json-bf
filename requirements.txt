# Runtime requirements
# --------------------
# The main script (cursed_format.py) requires no external libraries.
# It uses only the Python standard library.

# Optional development/test dependency:
pytest>=7.0
